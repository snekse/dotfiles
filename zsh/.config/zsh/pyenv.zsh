# pyenv — Python version manager
# Populated in Phase 4. pyenv is installed via Homebrew (brew install pyenv).
#
# export PYENV_ROOT="$HOME/.pyenv"
# export PATH="$PYENV_ROOT/bin:$PATH"
# eval "$(pyenv init -)"
# eval "$(pyenv virtualenv-init -)"

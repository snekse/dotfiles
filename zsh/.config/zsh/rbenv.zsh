# rbenv — Ruby version manager
# Populated in Phase 4. rbenv is installed via Homebrew (brew install rbenv ruby-build).
#
# Gem install location — installs gems into Homebrew prefix so no sudo needed
# export RBENV_ROOT="$(brew --prefix rbenv)"
# export GEM_HOME="$(brew --prefix)/opt/gems"
# export GEM_PATH="$(brew --prefix)/opt/gems"
# export PATH="$GEM_HOME/bin:$PATH"
#
# eval "$(rbenv init -)"

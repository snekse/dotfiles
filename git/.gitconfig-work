# Template — copy to ~/.gitconfig-work and fill in your real details.
# This file IS committed to the repo as a reference.
# The active file lives at ~/.gitconfig-work (not in the repo).
#
# Enable by uncommenting the matching includeIf block in .gitconfig.
[user]
  name = Your Name
  email = you@yourcompany.com
